[defaults]
inventory = inventory/hosts.yml
host_key_checking = False
retry_files_enabled = False
# Use YAML output formatting (built-in since ansible-core 2.13)
callback_result_format = yaml

[privilege_escalation]
become = True
become_method = sudo
